service: "service:svc"
labels:
    owner: ml-team
    stage: production
    version: "1.0.0"
    project: used-car-price-prediction

include:
    - "service.py"

python:
    packages:
        - "bentoml>=1.0.0"
        - "scikit-learn>=1.3.0"
        - "pandas>=2.0.0"
        - "numpy>=1.24.0"
        - "pydantic>=2.0.0"
    lock_packages: false

models:
    - "used_car_price_model:latest"
