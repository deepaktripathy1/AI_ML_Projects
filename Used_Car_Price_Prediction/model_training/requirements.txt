# Core ML Libraries
pandas >= 1.5.0
numpy >= 1.23.0
scikit-learn >= 1.2.0
scipy >= 1.9.0

# Visualization
matplotlib >= 3.6.0
seaborn >= 0.11.0

# Configuration and tracking
hydra-core >= 1.3.0
mlflow >= 2.0.0
dvc >= 3.0.0

# Model Serialization
joblib >= 1.2.0

# Logging and utilities
pyyaml >= 6.0
omegaconf >= 2.3.0
loguru >= 0.7

optuna >= 3.0.0

# Development dependencies
pytest>=7.0.0
black>=22.0.0
flake8>=5.0.0
pre-commit>=2.20.0